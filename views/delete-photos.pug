extends layout

block content 
  h1 Made it to delete photos!
  if profile.media 
    each img in profile.media
      .deleteImgContainer 
        button.deleteImgButton X
        img(
        class="profileMediaPic"
        src=`data:${img.img.contentType};base64,${Buffer.from(img.img.data).toString("base64")}`, 
        alt=img.altText)
  